{
  "name": "mediasoup-load-balancer",
  "version": "1.0.0",
  "description": "Load Balancer for Mediasoup Multi-Server Deployment",
  "main": "loadBalancer.js",
  "scripts": {
    "start": "node loadBalancer.js",
    "dev": "nodemon loadBalancer.js",
    "generate-password": "node -e \"const bcrypt = require('bcrypt'); const pwd = process.argv[1] || 'admin123'; bcrypt.hash(pwd, 10).then(hash => console.log('Password Hash:', hash));\"",
    "test": "curl http://localhost:3000/api/health"
  },
  "keywords": [
    "mediasoup",
    "load-balancer",
    "webrtc",
    "redis"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "ioredis": "^5.3.2",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}